# Copy to .env and fill in. Never commit .env.
DATABASE_URL=postgresql+psycopg://meridian:meridian_local_dev@localhost:5432/meridian
# Optional but recommended: CoinGecko demo API key (free tier).
# Without it, unauthenticated requests may be rate-limited or rejected.
COINGECKO_API_KEY=
